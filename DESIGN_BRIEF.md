# Westward Echo（西渡）— Design Brief

## 产品定位

Westward Echo 是一个 **AI 文化编译引擎**，不是翻译器。

核心差异：中文读者有一个共享文化图像库——看到"鬼节夜敲门"自动脑补月光像旧骨头的颜色照在雪上、纸钱灰在风中打旋、整个村庄屏息。英文读者没有这个库——他们只看到两个单词。西渡检测这些文化图像缺口，然后用**跨文化的通用感官材料**（冻肉、未融化的雪、沉默）重建场景。

产品名称：**西渡 / Westward Echo**
域名：**westwardecho.com**
当前版本：**v0.16.0**

## 当前状态

这是一个**已经上线**的产品——不是原型、不是 demo。内部测试阶段，后续公测。

### 核心数据
- 2301 章《地府叫我小先生》完整编译完成，59 次冷读全 PASS
- 775 章《无限恐怖》完整编译完成，16 次冷读全 PASS
- 3 份独立第三方审计通过
- MIT 协议开源，GitHub
- 209 个自动化测试
- 引擎速度：~1 分钟/章

### 技术架构（用户不需要看到，仅供理解产品能力）
四个 AI Agent 协作的 LangGraph 管道：
1. **READ**（双语文化情报员）——读原文，找文化缺口和感官图像差距
2. **WRITE**（类型小说作家）——用英文重建整个章节，不是逐句翻译
3. **READBACK**（冷读者，不知道自己读的是翻译）——诚实地盲评：困惑吗？无聊吗？继续读？
4. **FIX**（靶向编辑）——只修冷读者标记的问题，不做多余改动

### 核心功能
- 用户上传中文文件（支持 .txt .rtf .docx .md .epub）
- 自动分章 → 后台四 Agent 编译 → 完成后下载英文 EPUB / Markdown / 术语表
- API Key 可选填（内测阶段服务器提供默认 Key）
- 支持单语种和多语种并行

## 现有前端情况

### 技术实现
- 纯 HTML/CSS/JS 字符串，嵌在 Python `web_ui.py` 的一个 `PAGE = r"""..."""` 里
- 桌面布局：固定左侧 280px 侧边栏 + 右侧主内容区
- 移动端适配：680px 断点，侧边栏收为汉堡菜单
- Apple 设计语言风格：Inter 字体、蓝灰配色 (`#0071e3`, `#f5f5f7`), 卡片阴影、圆角边框

### 当前的问题
1. **Hero 区域太空了**——只有"西渡 / Westward Echo"大标题 + 一行小字，问一个陌生人 5 秒内不知道这个网站能干什么
2. **没有视觉演示**——行业里有 Google 翻译、DeepL，打开网页就懂了。西渡做的事情是新的，需要展示"编译 vs 翻译"的差异
3. **没有信任信号**——3000+ 章验证过、75+ 次冷读全 PASS、三份审计——这些数据在代码仓库和对话历史里，不在页面上
4. **空状态无聊**——没有编译记录时只显示文件图标和"上传内容开始编译"
5. **缺少"这不是另一个翻译工具"的第一印象**

## 当前页面代码

当前整个页面的 HTML/CSS/JS 都在一个 Python 字符串里：`src/web_ui.py` 中的 `PAGE = r"""..."""`。
你可以直接看仓库里的完整代码：
https://github.com/WenYu0306/Westward-Echo-/blob/main/src/web_ui.py

或者本地路径：`/Users/wenyudemac/Documents/dev/Westward Echo（西渡）/src/web_ui.py`

如果太长，这里是最核心的结构（纯 HTML，无框架，无 Tailwind）：

```html
<div class="app-shell">
  <!-- 左侧栏 280px -->
  <aside class="sidebar">
    <div class="sidebar-brand">西渡 / Westward Echo</div>
    <button>+ 新建编译</button>
    <div class="sidebar-jobs">编译记录列表（从 job 数据库加载）</div>
  </aside>

  <!-- 右侧主内容 -->
  <main>
    <!-- Hero 区 -->
    <header class="hero">
      <h1>西渡 / Westward Echo</h1>
      <p>Agent 以读者视角，感官画面重建</p>
    </header>

    <!-- 上传区 + 预览区 左右两列 -->
    <div class="translation-form-layout">
      <div class="form-left">
        卡片1：拖拽上传 .txt 文件
        卡片2：目标语言 / 内容类型 / API Key / 开始编译
      </div>
      <div class="form-right">
        预览面板：上传前显示"上传内容开始编译" / 编译中显示进度 / 完成后显示译文+术语表+下载
      </div>
    </div>
  </main>
</div>
```

## 当前页面截图描述

（你截两张图给 Kimi —— 一张桌面端完整页面，一张手机端。没有截图工具就用文字描述）

**桌面端**：左侧深色侧边栏、右侧大块空白 Hero 区只写了两行字、下面是一个上传框和几个下拉菜单。整个右侧 80% 是留白空间。预览区显示一个文件图标和"上传内容开始编译"。

**移动端**：侧边栏收成顶栏 + 汉堡菜单（左上角三横线），底下是一堆表单。Hero 标题变小，但整体感觉很空。

## 你觉得哪里不满意

- Hero 区太空了，一个陌生人打开不知道这网站能干嘛
- 不像一个产品，像一个内部工具
- 没有传达"这不是翻译器"的感觉
- 验证数据、信任信息全都不在页面上
- 空状态太无聊
- 整体缺乏品牌感

## 技术栈

- 纯 HTML + CSS + JS（不用 React、Vue、Tailwind、Bootstrap）
- 单页应用，全部内容在一个 HTML 字符串里（Python `r"""..."""` 模板）
- CSS 手写，Apple 设计语言风格（Inter 字体、`#f5f5f7` 底、`#0071e3` 蓝）
- 移动端已有一个 680px 媒体查询作为基础（可改）

## 使用场景

产品内测中的落地页 + 在线工具——用户打开网页直接上传文件开始编译，不需要注册/登录。目标用户是中文创作者（小说作者、剧本作者、游戏文案策划）想把内容出海到英文市场。

## 设计需求

### 必须保留
- 所有现有交互功能（上传、设置、轮询、侧边栏）
- 侧边栏布局
- 移动端响应
- 苹果风格审美（喜欢现在的配色）

### 你来做

打开 `https://westwardecho.com` 看现在的页面。然后用你的设计判断决定它需要什么。输出需要能放入 Python 的 `PAGE = r"""..."""` 模板字符串中。

## 设计约束
- 输出需要能融入现有的 Python `PAGE = r"""..."""` 模板字符串
- 保持 Inter 字体和现有配色体系（除非你有更好的方案）
- 680px 以下有移动端适配
- 所有交互功能保持不变（JS 代码不能动）

## 补充信息
- 创始人一个人完成了这个项目的全部代码、验证数据和产品化
- 这是一个从"翻译网文"起步但能力远超网文的引擎——科幻机甲、游戏本地化都能做
- 产品的真正价值在于"感官画面重建"这一独特能力，不是"便宜""快速"这些翻译服务通用卖点
